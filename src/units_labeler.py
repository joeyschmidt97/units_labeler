


def units_labeler(input, quantities):
    # Check if 'quantities' is an instance of str, if so, convert it to a list containing that string.
    quantities = [quantities] if isinstance(quantities, str) else quantities

    units_dict = {}

    for quant in quantities:
        pass

    return input, units_dict